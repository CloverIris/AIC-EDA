<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="AIC_EDA.Views.RecipeCompilerPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AIC_EDA.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.Resources>
        <DataTemplate x:Key="NodeTemplate">
            <Grid Padding="12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="40"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="80"/>
                    <ColumnDefinition Width="100"/>
                    <ColumnDefinition Width="120"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="{Binding Layer}" FontWeight="Bold" VerticalAlignment="Center"/>
                <StackPanel Grid.Column="1" Spacing="2">
                    <TextBlock Text="{Binding DisplayName}" FontWeight="SemiBold" FontSize="13"/>
                    <TextBlock Text="{Binding RecipeName}" FontSize="11" Opacity="0.6"/>
                    <TextBlock Text="{Binding Inputs}" FontSize="10" Opacity="0.5"/>
                </StackPanel>
                <TextBlock Grid.Column="2" Text="{Binding Count}" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                <StackPanel Grid.Column="3" Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center">
                    <TextBlock Text="{Binding PowerConsumption}" FontSize="11"/>
                    <TextBlock Text="kW" FontSize="11" Opacity="0.7"/>
                </StackPanel>
                <TextBlock Grid.Column="4" Text="{Binding Position}" VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="11" Opacity="0.6"/>
            </Grid>
        </DataTemplate>

        <DataTemplate x:Key="RawMaterialTemplate">
            <Grid Padding="8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="120"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="{Binding ItemName}" VerticalAlignment="Center"/>
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Right">
                    <TextBlock Text="{Binding RatePerMinute}" FontSize="11"/>
                    <TextBlock Text="/min" FontSize="11" Opacity="0.7"/>
                </StackPanel>
            </Grid>
        </DataTemplate>
    </Page.Resources>

    <Grid Padding="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 标题 -->
        <StackPanel Grid.Row="0" Spacing="8" Margin="0,0,0,16">
            <TextBlock Text="Recipe Compiler (配方综合器)" Style="{StaticResource TitleLargeTextBlockStyle}"/>
            <TextBlock Text="从目标产物反向构建生产树，计算最优设备配置" Opacity="0.7"/>
        </StackPanel>

        <!-- 控制面板 -->
        <Grid Grid.Row="1" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <ComboBox Grid.Column="0" 
                      Header="目标产物" 
                      PlaceholderText="选择要生产的物品"
                      ItemsSource="{x:Bind ViewModel.FinalProducts}"
                      SelectedItem="{x:Bind ViewModel.SelectedTarget, Mode=TwoWay}"
                      DisplayMemberPath="Name"
                      Margin="0,0,12,0"/>

            <NumberBox Grid.Column="1"
                       Header="目标产量 (/min)"
                       Value="{x:Bind ViewModel.TargetRate, Mode=TwoWay}"
                       Minimum="1"
                       Maximum="10000"
                       SpinButtonPlacementMode="Inline"
                       Margin="0,0,12,0"/>

            <Button Grid.Column="2"
                    Content="编译 (Compile)"
                    Command="{x:Bind ViewModel.CompileCommand}"
                    Style="{StaticResource AccentButtonStyle}"
                    VerticalAlignment="Bottom"
                    Margin="0,0,8,0"/>

            <Button Grid.Column="3"
                    Content="优化布局"
                    Command="{x:Bind ViewModel.OptimizeLayoutCommand}"
                    VerticalAlignment="Bottom"
                    Margin="0,0,8,0"/>

            <Button Grid.Column="4"
                    Content="检测瓶颈"
                    Command="{x:Bind ViewModel.DetectBottlenecksCommand}"
                    VerticalAlignment="Bottom"/>
        </Grid>

        <!-- 状态栏 -->
        <InfoBar Grid.Row="2"
                 Message="{x:Bind ViewModel.StatusMessage, Mode=OneWay}"
                 IsOpen="True"
                 Severity="Informational"
                 Margin="0,0,0,12"/>

        <!-- 结果区 -->
        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- 左侧：节点列表 -->
            <Grid Grid.Column="0" Margin="0,0,12,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                
                <StackPanel Grid.Row="0" Orientation="Horizontal" Spacing="16" Margin="0,0,0,8">
                    <TextBlock Text="生产节点" Style="{StaticResource SubtitleTextBlockStyle}"/>
                    <TextBlock VerticalAlignment="Center" Opacity="0.7">
                        <Run Text="总设备: "/><Run Text="{x:Bind ViewModel.TotalMachines, Mode=OneWay}"/>
                    </TextBlock>
                    <TextBlock VerticalAlignment="Center" Opacity="0.7">
                        <Run Text="总功率: "/><Run Text="{x:Bind ViewModel.TotalPower, Mode=OneWay}"/><Run Text="kW"/>
                    </TextBlock>
                </StackPanel>

                <ListView Grid.Row="1"
                          ItemsSource="{x:Bind ViewModel.Nodes}"
                          ItemTemplate="{StaticResource NodeTemplate}"
                          BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                          BorderThickness="1"
                          CornerRadius="8"/>
            </Grid>

            <!-- 右侧：原始资源 -->
            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Text="原始资源需求" Style="{StaticResource SubtitleTextBlockStyle}" Margin="0,0,0,8"/>
                <ListView Grid.Row="1"
                          ItemsSource="{x:Bind ViewModel.RawMaterials}"
                          ItemTemplate="{StaticResource RawMaterialTemplate}"
                          BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                          BorderThickness="1"
                          CornerRadius="8"/>
            </Grid>
        </Grid>
    </Grid>
</Page>
