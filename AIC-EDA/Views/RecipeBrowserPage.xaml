<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="AIC_EDA.Views.RecipeBrowserPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AIC_EDA.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.Resources>
        <DataTemplate x:Key="ItemTemplate">
            <Grid Padding="8">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <TextBlock Text="{Binding Name}" FontWeight="SemiBold" FontSize="14"/>
                <TextBlock Grid.Row="1" Text="{Binding NameEN}" FontSize="11" Opacity="0.6"/>
            </Grid>
        </DataTemplate>

        <DataTemplate x:Key="RecipeTemplate">
            <Grid Padding="8">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <TextBlock Text="{Binding Name}" FontWeight="SemiBold" FontSize="13"/>
                <TextBlock Grid.Row="1" Text="{Binding Machine}" FontSize="11" Opacity="0.7"/>
                <StackPanel Grid.Row="2" Orientation="Horizontal" Opacity="0.5">
                    <TextBlock Text="{Binding Duration}" FontSize="11"/>
                    <TextBlock Text="s" FontSize="11"/>
                </StackPanel>
            </Grid>
        </DataTemplate>
    </Page.Resources>

    <Grid Padding="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 标题和搜索 -->
        <StackPanel Grid.Row="0" Spacing="12" Margin="0,0,0,16">
            <TextBlock Text="配方浏览器 (Recipe Browser)" Style="{StaticResource TitleLargeTextBlockStyle}"/>
            <TextBlock Text="浏览所有配方和物品，查看生产链关系" Opacity="0.7"/>
            <TextBox PlaceholderText="搜索配方或物品..." 
                     Text="{x:Bind ViewModel.SearchText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                     Width="300" HorizontalAlignment="Left"/>
        </StackPanel>

        <!-- 内容区 -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="280"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- 左侧：物品列表 -->
            <Grid Grid.Column="0" Margin="0,0,12,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Text="物品列表" Style="{StaticResource SubtitleTextBlockStyle}" Margin="0,0,0,8"/>
                <ListView Grid.Row="1"
                          ItemsSource="{x:Bind ViewModel.Items}"
                          SelectedItem="{x:Bind ViewModel.SelectedItem, Mode=TwoWay}"
                          ItemTemplate="{StaticResource ItemTemplate}"
                          BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                          BorderThickness="1"
                          CornerRadius="8"/>
            </Grid>

            <!-- 右侧：配方详情 -->
            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Text="生产配方" Style="{StaticResource SubtitleTextBlockStyle}" Margin="0,0,0,8"/>
                <ListView Grid.Row="1"
                          ItemsSource="{x:Bind ViewModel.ItemRecipes}"
                          ItemTemplate="{StaticResource RecipeTemplate}"
                          BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                          BorderThickness="1"
                          CornerRadius="8"/>

                <TextBlock Grid.Row="2" Text="作为原料被使用" Style="{StaticResource SubtitleTextBlockStyle}" Margin="0,16,0,8"/>
                <ListView Grid.Row="3"
                          ItemsSource="{x:Bind ViewModel.ItemUsages}"
                          ItemTemplate="{StaticResource RecipeTemplate}"
                          BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                          BorderThickness="1"
                          CornerRadius="8"/>
            </Grid>
        </Grid>
    </Grid>
</Page>
