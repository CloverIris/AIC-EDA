<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="AIC_EDA.Views.LayoutPreviewPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AIC_EDA.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 标题 -->
        <StackPanel Grid.Row="0" Spacing="8" Margin="0,0,0,16">
            <TextBlock Text="FloorPlan-EF (布图规划器)" Style="{StaticResource TitleLargeTextBlockStyle}"/>
            <TextBlock Text="2D/3D 工厂布局预览与优化" Opacity="0.7"/>
        </StackPanel>

        <!-- 工具栏 -->
        <CommandBar Grid.Row="1" DefaultLabelPosition="Right" Margin="0,0,0,12">
            <AppBarButton Icon="Refresh" Label="重新布局" Click="RefreshLayout_Click"/>
            <AppBarButton Icon="ZoomIn" Label="放大" Click="ZoomIn_Click"/>
            <AppBarButton Icon="ZoomOut" Label="缩小" Click="ZoomOut_Click"/>
            <AppBarSeparator/>
            <AppBarToggleButton Icon="TwoPage" Label="显示连接" IsChecked="True" x:Name="ShowConnectionsToggle"/>
            <AppBarToggleButton Icon="AllApps" Label="显示网格" IsChecked="True" x:Name="ShowGridToggle"/>
        </CommandBar>

        <!-- 画布区 -->
        <Border Grid.Row="2"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="8"
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
            <Grid>
                <Canvas x:Name="LayoutCanvas" 
                        Background="Transparent"
                        PointerPressed="Canvas_PointerPressed"
                        PointerMoved="Canvas_PointerMoved"
                        PointerReleased="Canvas_PointerReleased"/>
                
                <TextBlock x:Name="PlaceholderText"
                           Text="请先前往 Recipe Compiler 页面编译生产图"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           Opacity="0.5"
                           FontSize="16"/>
            </Grid>
        </Border>
    </Grid>
</Page>
